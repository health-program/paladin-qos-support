<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paladin.qos.dynamic.mapper.migration.GongweiDataMigrateMapper">
	
	<select id="selectData200001">
		SELECT * FROM phis_chss.v_report_year_blood
		WHERE reportyear IN
		<foreach collection="years" item="year" separator="," open="(" close=")">
            #{year}
        </foreach>
	</select>

</mapper>
